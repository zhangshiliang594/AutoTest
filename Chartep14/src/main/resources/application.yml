server:
  port: 8888

logging:
  path: logs
  file: wanglin.log

spring:
  application:
    name: userManger
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/coures?serverTimezone=UTC
    username: root
    password: root

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
  - mapper/*
